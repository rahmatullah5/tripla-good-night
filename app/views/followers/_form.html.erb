<%= form_with(model: follower) do |form| %>
  <% if follower.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(follower.errors.count, "error") %> prohibited this follower from being saved:</h2>

      <ul>
        <% follower.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :follower_id, style: "display: block" %>
    <%= form.number_field :follower_id %>
  </div>

  <div>
    <%= form.label :followee_id, style: "display: block" %>
    <%= form.number_field :followee_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
